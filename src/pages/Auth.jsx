import React,{useState} from 'react';
import {api,setToken} from '../api';
import {Sparkles,ArrowRight,Eye,EyeOff} from 'lucide-react';
export default function Auth({onAuth}){const [mode,setMode]=useState('login'),[form,setForm]=useState({fullName:'',email:'',password:'',college:'',degree:'',graduationYear:'2027',primaryCareerInterest:'Software Engineering'}),[show,setShow]=useState(false),[loading,setLoading]=useState(false),[error,setError]=useState('');
 const change=e=>setForm({...form,[e.target.name]:e.target.value});
 async function submit(e){e.preventDefault();setError('');setLoading(true);try{const d=await api(mode==='login'?'/api/auth/login':'/api/auth/register',{method:'POST',body:JSON.stringify(form)});setToken(d.token);onAuth(d.user)}catch(e){setError(e.message)}finally{setLoading(false)}}
 return <div className="auth-screen"><div className="auth-card"><div className="auth-brand"><div className="brand-mark"><Sparkles size={18}/></div><div><b>CareerSync</b><span>AI · STUDENT</span></div></div><span className="eyebrow">CARRER-X STUDENT</span><h1>{mode==='login'?'Welcome back.':'Build your proof.'}</h1><p>{mode==='login'?'Continue your verified career-readiness journey.':'Create your profile and turn what you can do into career evidence.'}</p>{error&&<div className="auth-error">{error}</div>}<form onSubmit={submit}>{mode==='register'&&<><input name="fullName" placeholder="Full name" value={form.fullName} onChange={change} required/><input name="college" placeholder="College / Institution" value={form.college} onChange={change} required/><input name="degree" placeholder="Degree / Program" value={form.degree} onChange={change} required/></>}<input name="email" type="email" placeholder="Email address" value={form.email} onChange={change} required/><div className="password-field"><input name="password" type={show?'text':'password'} placeholder="Password" value={form.password} onChange={change} required/><button type="button" onClick={()=>setShow(!show)}>{show?<EyeOff size={17}/>:<Eye size={17}/>}</button></div><button className="primary-btn auth-submit" disabled={loading}>{loading?'Please wait…':mode==='login'?'Sign in':'Create account'} <ArrowRight size={17}/></button></form><button className="auth-switch" onClick={()=>{setMode(mode==='login'?'register':'login');setError('')}}>{mode==='login'?"Don't have an account? Create one":"Already have an account? Sign in"}</button></div></div>}
